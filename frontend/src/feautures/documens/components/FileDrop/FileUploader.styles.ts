import type { SxProps } from "@mui/material";

interface FileUploaderStyles {
  container: SxProps;
}

export const fileUploaderStyles: FileUploaderStyles = {
  container: { maxWidth: 560, mx: "auto", mt: 4 },
};
